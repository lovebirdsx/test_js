{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "tsc -b",
    "watch": "tsc -b -w",
    "prestart": "yarn build",
    "start": "node dist/src/main.js",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "common": "*"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
