{
  "name": "test_framework",
  "version": "1.0.0",
  "description": "Prototype of universe editor's test framework",
  "author": "lovebird",
  "license": "ISC",
  "scripts": {
    "start": "rm -rf typescript_out && tsc-watch --onSuccess \"node typescript_out/src/main.js\"",
    "test": "rm -rf typescript_out && tsc-watch --onSuccess \"node typescript_out/src/test.js\"",
    "case": "rm -rf typescript_out && tsc-watch --onSuccess \"node typescript_out/src/test.js -t 'stcp send obj'\""
  },
  "dependencies": {
    "@types/node": "^20.5.6",
    "source-map": "^0.6.1"
  }
}
